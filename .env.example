# Green RV Scheduling API secrets
# Copy these variable names into Render Environment Variables and set real values there.
# Do not commit real secret values to this repository.
GREENRV_SCHEDULING_API_KEY=<set-in-render-only>
GREENRV_API_ENCRYPTION_KEY=<set-in-render-only>
